<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.safetyalert.app"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="21"
        android:targetSdkVersion="33" />

    <!-- Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <permission
        android:name="com.safetyalert.app.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.safetyalert.app.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION" />

    <application
        android:allowBackup="true"
        android:appComponentFactory="androidx.core.app.CoreComponentFactory"
        android:debuggable="true"
        android:extractNativeLibs="true"
        android:icon="@mipmap/ic_launcher"
        android:label="SafetyAlert"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:testOnly="true"
        android:theme="@style/Theme.SafetyAlert" >

        <!-- Splash Screen (Launcher Activity) -->
        <activity
            android:name="com.safetyalert.app.activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SafetyAlert" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Register Activity -->
        <activity
            android:name="com.safetyalert.app.activities.RegisterActivity"
            android:exported="false" />

        <!-- Login Activity -->
        <activity
            android:name="com.safetyalert.app.activities.LoginActivity"
            android:exported="false" />

        <!-- Home Screen / Main Activity -->
        <activity
            android:name="com.safetyalert.app.MainActivity"
            android:exported="false" />

        <!-- Other Screens -->
        <activity
            android:name="com.safetyalert.app.activities.ContactsActivity"
            android:exported="false" />
        <activity
            android:name="com.safetyalert.app.activities.MapActivity"
            android:exported="false" />
        <activity
            android:name="com.safetyalert.app.activities.AlertStatusActivity"
            android:exported="false" />

        <!-- Foreground Service -->
        <service
            android:name="com.safetyalert.app.services.LocationService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <activity
            android:name="com.google.android.gms.common.api.GoogleApiActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="com.safetyalert.app.androidx-startup"
            android:exported="false" >
            <meta-data
                android:name="androidx.emoji2.text.EmojiCompatInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.lifecycle.ProcessLifecycleInitializer"
                android:value="androidx.startup" />
        </provider>
    </application>

</manifest>